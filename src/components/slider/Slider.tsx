import React from "react";
import { Section } from "@/components/section/Section";

export const Slider = () => {
    return (
        <Section>
            <div className="slider">
                <h1>Slider Blank</h1>
            </div>
        </Section>
    );
};
